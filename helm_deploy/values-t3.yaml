# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

image:
  repository: quay.io/hmpps/hmpps-auth
  tag: latest
  pullPolicy: IfNotPresent
  port: 8080
  livenessProbePath: /auth/ping
  readinessProbePath: /auth/ping
