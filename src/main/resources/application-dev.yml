spring:

  h2:
    console:
      enabled: true

nomis:
  flyway:
    locations: db/nomis,db/dev/data/nomis

auth:
  flyway:
    locations: db/auth,db/dev/data/auth
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect

application:
  smoketest:
    enabled: true
  verify-email:
    enabled: true
  # test key which doesn't send emails, but is automatically accepted by notify
  notify:
    key: testkey-992308b2-1e8f-4667-816b-8bdde781a4ac-9c211cf4-96d1-4c30-848b-8f172bca0b41
